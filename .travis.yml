sudo: false

language: java
jdk: oraclejdk8

cache:
  directories:
    - '$HOME/.m2/repository'

script:
  - mvn clean install -B && mvn clean -B
after_test:
 - travis-artifacts upload --target-path target/
